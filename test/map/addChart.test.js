// import createMap from '../../test/createMap';

describe('addChart', () => {
  test('should return correct layer object fill layer', () => {
    // createMap([], () => { });
    expect(1).toEqual(1);
  });
});
